{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main chat window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-agent-status",
    "allow-start-chat",
    "allow-send-message",
    "allow-close-agent"
  ]
}
